@model IEnumerable<Proyecto_Final_Web.Models.Role>

@{
    ViewData["Title"] = "Index";
}



<div class="hero-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10 col-md-12">
                <div class="form-container p-4 p-md-5">
                    <div class="form-header">
                        <h1><i class="fas fa-user-tag me-3"></i>Lista de Roles</h1>
                        <p>Administra los roles de usuario en el sistema</p>
                    </div>

                    <div class="d-flex justify-content-end mb-4">
                        @if (User.IsInRole("Gerente"))
                        {
                            <a asp-action="Create" class="btn btn-primary">
                                <i class="fas fa-plus-circle me-2"></i>
                                Crear Nuevo Rol
                            </a>
                        }
                    </div>

                    <div class="table-responsive">
                        @if (Model != null && Model.Any())
                        {
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th class="th-icon-nombre">
                                            @Html.DisplayNameFor(model => model.Nombre)
                                        </th>
                                        <th class="th-icon-descripcion">
                                            @Html.DisplayNameFor(model => model.Descripcion)
                                        </th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td data-label="@Html.DisplayNameFor(model => model.Nombre)">
                                                @Html.DisplayFor(modelItem => item.Nombre)
                                            </td>
                                            <td data-label="@Html.DisplayNameFor(model => model.Descripcion)">
                                                @Html.DisplayFor(modelItem => item.Descripcion)
                                            </td>
                                            <td data-label="Acciones">
                                                <div class="d-flex justify-content-center justify-content-md-start">
                                                    @if (User.IsInRole("Gerente"))
                                                    {
                                                        <a asp-action="Edit" asp-route-id="@item.IdRol" class="btn-action-edit">
                                                            <i class="fas fa-edit"></i> Editar
                                                        </a>
                                                        <a asp-action="Details" asp-route-id="@item.IdRol" class="btn-action-details">
                                                            <i class="fas fa-info-circle"></i> Detalles
                                                        </a>
                                                        <a asp-action="Delete" asp-route-id="@item.IdRol" class="btn-action-delete">
                                                            <i class="fas fa-trash-alt"></i> Eliminar
                                                        </a>
                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                        else
                        {
                            <div class="alert alert-info text-center" role="alert">
                                <h4 class="alert-heading">¡No hay roles registrados!</h4>
                                <p>Actualmente no hay roles en el sistema. Puedes crear uno nuevo usando el botón de arriba.</p>
                                <hr>
                                <p class="mb-0">Empieza a organizar los permisos de tu aplicación.</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
