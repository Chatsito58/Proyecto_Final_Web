@model IEnumerable<Proyecto_Final_Web.Models.EstadosFactura>

@{
    ViewData["Title"] = "Index";
}



<div class="hero-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10 col-md-12">
                <div class="form-container p-4 p-md-5">
                    <div class="form-header">
                        <h1><i class="fas fa-file-invoice-dollar me-3"></i>Lista de Estados de Factura</h1>
                        <p>Administra los estados de las facturas en el sistema</p>
                    </div>

                    <div class="d-flex justify-content-end mb-4">
                        @if (User.IsInRole("Empleado") || User.IsInRole("Administrador") || User.IsInRole("Gerente"))
                        {
                            <a asp-action="Create" class="btn btn-primary">
                                <i class="fas fa-plus-circle me-2"></i>
                                Crear Nuevo Estado
                            </a>
                        }
                    </div>

                    <div class="table-responsive">
                        @if (Model != null && Model.Any())
                        {
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th class="th-icon-nombre">
                                            @Html.DisplayNameFor(model => model.Nombre)
                                        </th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td data-label="@Html.DisplayNameFor(model => model.Nombre)">
                                                @Html.DisplayFor(modelItem => item.Nombre)
                                            </td>
                                            <td data-label="Acciones">
                                                <div class="d-flex justify-content-center justify-content-md-start">
                                                    @if (User.IsInRole("Empleado") || User.IsInRole("Administrador") || User.IsInRole("Gerente"))
                                                    {
                                                        <a asp-action="Edit" asp-route-id="@item.IdEstadoFactura" class="btn-action-edit">
                                                            <i class="fas fa-edit"></i> Editar
                                                        </a>
                                                        <a asp-action="Delete" asp-route-id="@item.IdEstadoFactura" class="btn-action-delete">
                                                            <i class="fas fa-trash-alt"></i> Eliminar
                                                        </a>
                                                    }
                                                    <a asp-action="Details" asp-route-id="@item.IdEstadoFactura" class="btn-action-details">
                                                        <i class="fas fa-info-circle"></i> Detalles
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        }
                        else
                        {
                            <div class="alert alert-info text-center" role="alert">
                                <h4 class="alert-heading">¡No hay estados de factura registrados!</h4>
                                <p>Actualmente no hay estados de factura en el sistema que cumplan con los criterios de visualización.</p>
                                <hr>
                                <p class="mb-0">Puedes crear un nuevo estado de factura si tienes los permisos adecuados.</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
